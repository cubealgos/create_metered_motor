/** The motor's client-only rendering and goggles overlay: the shaft's visual, its fallback renderer and the {@code TooltipBehaviour} that draws the goggles readout (MOTOR-REQ-012, MOTOR-REQ-013). */
package metered_motor.client.visual;
